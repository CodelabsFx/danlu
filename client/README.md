Danlu Client (Vite + React)

Quick start:

```bash
cd client
npm install
npm run dev
```

Environment:
- `VITE_API_URL` can be set to the backend URL (e.g., `http://localhost:3000`).

This is a minimal scaffold — I'll wire routes, auth, and POS UI next if you want.
